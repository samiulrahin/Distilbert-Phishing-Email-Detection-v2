# Processed Data Boundary

Intermediate cleaned data is local experiment material and is not committed. The reproducible train, validation, and test outputs are generated by `ml.preprocess` under `data/splits/`.

